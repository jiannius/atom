<atom:docs.example
title="Basic"
description="Avatar + name + email row. Pass name, email and avatar explicitly; left unset they default to the authenticated user (auth()->user()). size matches the avatar sizes."
view="atom::docs.demos.profile.basic"/>
