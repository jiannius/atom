<atom:docs.example
title="Basic"
description="A full scheduling surface (built on @event-calendar) with built-in period (month/week/day) and mode (calendar/timeline) switchers and a date-picker jump. Events are pushed in at runtime via the add-calendar-events window event (or a Livewire component); shown here empty. Restrict the switchers with the periods and modes props."
view="atom::docs.demos.calendar.basic"/>
